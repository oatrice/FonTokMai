# งานที่ต้องทำ (Batch E: UX, Insights & Crowdsourcing)

- [x] **1. เพิ่มฐานข้อมูล Ground Truth Feedback (Issue #39)**
  - [x] เพิ่ม `UserFeedback` Model ใน `models.py`
  - [x] เพิ่ม Abstract Method `save_feedback` ใน `base.py`
  - [x] Implement `save_feedback` ใน `sqlite.py`
  - [x] Implement `save_feedback` ใน `firestore.py`
  - [x] เขียน Unit Test (Red -> Green -> Refactor)
- [x] **2. เพิ่ม Insights API Comparison (Issue #42)**
  - [x] เพิ่มฟังก์ชัน `compare_all_apis` ใน `WeatherManager` ให้รันขนานด้วย `asyncio.gather`
  - [x] สร้างการจัดรูปแบบตาราง/ข้อความเปรียบเทียบ
  - [x] เขียน Unit Test (Red -> Green -> Refactor)
- [x] **3. การส่งแจ้งเตือน All-Clear & ปรับแต่ง UI (Issue #41 & #39 & #42)**
  - [x] อัปเดต `scheduler_tasks.py` ให้มี Logic ตรวจสอบถ้า `max_rain < Threshold` แต่เคยเตือนไปแล้ว (`last_alert_max_rain > 0.0`) ให้ส่ง All-Clear
  - [x] ปรับแก้ UI ปุ่มผ่านฟังก์ชัน `get_radar_inline_keyboard` หรือเพิ่มในจังหวะที่แจ้งเตือน:
    - [x] ปุ่ม ❌ แจ้งเตือนผิดพลาด (ฝนไม่ตก)
    - [x] ปุ่ม 📊 เทียบข้อมูล 3 API
  - [x] เขียน Unit Test ทดสอบ Scheduler (Red -> Green -> Refactor)
- [x] **4. อัปเดต Webhook จัดการ Callbacks**
  - [x] ใน `webhook.py` เพิ่มการจัดการ callback สำหรับ `fb_falsealarm_...`
  - [x] ใน `webhook.py` เพิ่มการจัดการ callback สำหรับ `compare_api_...`
- [x] **5. ตรวจสอบความถูกต้องและสร้าง Walkthrough**
  - [x] ตรวจสอบการรันร่วมกับบอทด้วย Mock state
  - [x] ตรวจสอบว่าตารางฐานข้อมูลและเอกสารถูกสร้างเรียบร้อย
  - [x] จัดทำ Walkthrough.md และใช้เครื่องมือ `notify_task_complete`
